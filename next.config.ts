import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com', 'pixabay.com', 'via.placeholder.com'], // ✅ Add your domain here
  },
};

export default nextConfig;
